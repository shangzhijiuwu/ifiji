<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.iszhenyu.ifiji.dao.mapper.UserMapper">

    <insert id="insert" parameterType="User">
      insert into sys_user
        (username, password_hash, password_salt, mobile_number, email, avatar_url, status, deleted, created_at, updated_at)
      values
        (#{username}, #{passwordHash}, #{passwordSalt}, #{mobileNumber}, #{email}, #{avatarUrl}, #{status}, #{deleted}, #{createdAt}, #{updatedAt})
    </insert>

</mapper>